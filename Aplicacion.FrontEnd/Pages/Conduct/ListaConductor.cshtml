@page
@model Aplicacion.FrontEnd.Pages.ListaConductorModel
@{
}
<div class="row">
    <div class="p-3 mb-2 bg-light text-white">
<h1 class="text-secondary">Conductores</h1>
<a class="btn btn-success" asp-page="./EditarConductor">
    <i class="fa-regular fa-square-plus"></i><em> NUEVO CONDUCTOR</em>
</a>
<table class="table">
    <th>
            <th>Cedula</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Celular</th>
    </th>
    @foreach (var item in Model.conduc)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Cedula</td>
            <td>@item.Nombre</td>
            <td>@item.Apellido</td>
            <td>@item.Celular</td>
            <td>
                <a class="btn btn-outline-success" asp-page="./DetalleConductor" asp-route-ConductorId="@item.Id">
                    <i class="fas fa-info-circle"></i>
                    @* <i class="fa-brands fa-windows"></i> *@
                </a>
                <a class="btn btn-outline-success" asp-page="./EditarConductor" asp-route-ConductorId="@item.Id">
                    <i class="fa-regular fa-pen-to-square"></i>
                </a>
                
            </td>
            
        </tr>
    }
</table>
    </div>
</div>